spring:
  datasource:
    password: root
    username: root
    url: jdbc:mysql://localhost:3306/provider?useTimezone=true&serverTimezone=UTC
  jpa:
    hibernate: globally_quoted_identifiers=true
    ddl-auto: create

