spring:
  datasource:
    password: root
    username: root
    url: jdbc:mysql://localhost:3306/provider?useTimezone=true&serverTimezone=UTC
  jpa:
  show-sql:
    hibernate: format_sql=true 
    ddl-auto: update

